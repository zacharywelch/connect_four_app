<p id="notice"><%= notice %></p>

<p>
  <strong>Player one:</strong>
  <%= @game.player_one %>
</p>

<p>
  <strong>Player two:</strong>
  <%= @game.player_two %>
</p>

<%= render 'games/board' %>

<%= content_tag :h3, "#{@game.turn} won!" if @game.over? %>
<%= content_tag :h3, "Your move #{@game.turn}" unless @game.over? %>

<%= render 'moves/form' unless @game.over? %>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
